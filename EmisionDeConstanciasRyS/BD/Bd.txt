CREATE DATABASE  IF NOT EXISTS `ruts`;
CREATE TABLE `ruts`.`alumnos` (
  `NoControl` VARCHAR(8) NOT NULL,
  `Contraseña` VARCHAR(45) NULL DEFAULT NULL,
  `Nombres` VARCHAR(45) NULL DEFAULT NULL,
  `APaterno` VARCHAR(45) NULL DEFAULT NULL,
  `AMaterno` VARCHAR(45) NULL DEFAULT NULL,
  `Especialidad` VARCHAR(45) NULL DEFAULT NULL,
  `Email` VARCHAR(45) NULL DEFAULT NULL,
  PRIMARY KEY (`NoControl`));

para llenar la tabla seguir las imagenes

1) Click derecho sobre la tabla y elijir "Tabla Data Import Wizard"
2) Seleccionar el archivo adjunto Datos Bd.csv
3) Dejar como en la imagen
4) Cambiar a "latin1 (iso8859-1)" y solo marcar las indicadas (demarcar todas las demas)
5) Next y esperar que acabe